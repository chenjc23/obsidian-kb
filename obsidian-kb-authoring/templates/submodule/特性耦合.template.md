---
title: {{title}}
type: submodule
created: {{created}}
updated: {{updated}}
repo: {{repo}}
sources:
  - <!-- 填:path:func() durable 证据,不带行号 -->
confidence: <!-- 填:high|medium|low -->
status: active
---

<!-- 外部知识:隐式耦合(共享状态/静态初始化顺序/时序/事件)与「解耦边界与注意」多不显现于调用图,需设计文档/领域知识补充;外部未提供则据已有知识生成。 -->
# 特性耦合：{{title}}
> <!-- 填:一句话概括本子模块与其它特性的耦合;与其它特性无显著耦合则删除本文件 -->

## 耦合关系图
```mermaid
flowchart LR
    B([本子模块]) -- 共享状态 --> F1[特性A]
    F2[特性B] -- 事件 --> B
```
<!-- 填:按真实耦合改写;边标注耦合方式(共享状态/事件/配置/直接调用)与方向,节点为耦合的特性/子模块。重点画调用图看不出的隐式耦合(全局/静态、时序、事件)。 -->

## 耦合的特性
<!-- 填:逐条列耦合对象 + 耦合点——
- 对象:哪个特性/子模块,跨子模块链 [[repos/{repo}/submodules/{topic}/上下文]]
- 耦合点:具体耦合在哪(共享的全局变量/单例、约定的调用顺序、共享配置、同一段内存/锁) -->

## 耦合方式
<!-- 填:逐条说清机制 + 方向——
- 机制:共享状态(全局/静态/单例)、事件/回调、配置、直接调用之一
- 方向:谁影响谁,单向还是双向
- C++ 隐式项:静态初始化顺序依赖、线程间共享数据的锁约定、宏/编译期开关 -->

## 解耦边界与注意
<!-- 填:改本子模块时会牵连什么、必须同步查什么——
- 牵连点:顺着上面的耦合点,改了会波及哪些对象
- 同步检查:哪些地方必须一起改/一起测,漏了会静默出错(数据竞争、初始化顺序、契约不兼容) -->
