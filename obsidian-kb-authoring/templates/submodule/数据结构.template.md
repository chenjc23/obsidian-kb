---
title: {{title}}
type: submodule
created: {{created}}
updated: {{updated}}
repo: {{repo}}
sources:
  - <!-- 填:path:func() durable 证据,不带行号 -->
confidence: <!-- 填:high|medium|low -->
status: active
---

# 数据结构：{{title}}
> <!-- 填:一句话概括子模块内部的数据结构/表设计 -->

## 内部数据结构
<!-- 填:每个子模块私有的结构/类/枚举一个三级标题块,可复用的提升到 [[repos/{repo}/data-models]] 只留指针;例:
### {结构名}
| 字段 | 类型 | 含义 | 约束 |
|---|---|---|---|
- 约束列写:取值范围/不变式/所有权(谁 new 谁 delete、裸指针 vs 智能指针)/线程可见性
- 只写本子模块私有;跨子模块共用的结构提升到 data-models -->

## 数据表设计
<!-- optional:有持久化才写,无则删本节。逐表说清——
- 表结构:关键列 + 类型 + 主键
- 索引:哪些列建索引、对应什么查询模式
- 关键约束:唯一/外键/非空等硬约束 -->

## 与全局数据模型的关系
<!-- 填:本子模块复用/引用的全局结构,逐条链,只留指针不重抄字段——
- 复用全局结构:[[repos/{repo}/data-models#结构名]]
- 跨边界 schema:[[global/contracts/X]] -->
